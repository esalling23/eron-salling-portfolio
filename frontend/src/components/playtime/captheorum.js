
const CAPTheorum = () => {

  // To explain the cap theorum we should have an interactive system displayed

  // users can perform actions, like a simultaneous read/write
  // and see the resulting loss of Consistency, Availability, or Partition tolerance

  // 
}
