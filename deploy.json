{
  "apps" : [{
    "name": "Porfolio Website",
    "script": "server.js",
    "env_production": {
      "NODE_ENV": "production"
    },
    "env_staging": {
      "NODE_ENV": "staging"
    }
  }],
  "deploy" : {

    "sdk-production" : {
      "user": "node",
      "host": "165.227.120.49",
      "repo": "https://github.com/esalling23/el-web-sdk.git",
      "ref": "origin/master",
      "path": "/srv/framework",
      "pre-deploy-local" : "echo 'Cloning and running npm install. Be patient.'",
      "post-deploy" : "npm install ; npm update ; grunt compile --sites=porfolio-website --env production ; pm2 restart 'server' -- porfolio-website --env production",
      "env": {
        "NODE_ENV": "production"
      }
    },

    "production" : {
      "user": "node",
      "host": "165.227.120.49",
      "repo": "https://github.com/esalling23/erica-salling-portfolio.git",
      "ref": "origin/master",
      "path": "/srv/website",
      "pre-deploy-local" : "echo 'NPM linking repo-name and restarting app.';",
      "post-deploy" : "npm i ; cd /srv/framework/source/ ; grunt compile --sites=porfolio-website --env production ; NODE_ENV=production pm2 restart server.js -- porfolio-website --env production"
    }

  }
}